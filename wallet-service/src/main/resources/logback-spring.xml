<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
      Structured JSON logs to stdout so the free host (Render/Railway/Fly) captures
      them and makes them publicly viewable. Each line carries the correlation id
      and any domain fields put on the MDC.
    -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>correlation_id</includeMdcKeyName>
            <includeMdcKeyName>user</includeMdcKeyName>
            <includeMdcKeyName>event</includeMdcKeyName>
            <includeMdcKeyName>transfer_id</includeMdcKeyName>
            <includeMdcKeyName>wallet_id</includeMdcKeyName>
            <includeMdcKeyName>idempotency_key</includeMdcKeyName>
            <includeMdcKeyName>amount_paise</includeMdcKeyName>
            <fieldNames>
                <timestamp>ts</timestamp>
                <message>msg</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
        </encoder>
    </appender>

    <!-- Plain console for local dev when JSON is noisy -->
    <appender name="PLAIN" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} corr=%X{correlation_id} event=%X{event} - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="local">
        <root level="INFO">
            <appender-ref ref="PLAIN"/>
        </root>
    </springProfile>

    <springProfile name="!local">
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>
</configuration>
